services:
  docs:
    image: mr_tracker_local_docs
    container_name: mr_tracker_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - /app/.venv
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./mr_tracker:/app/mr_tracker:z
    ports:
      - '9000:9000'
    command: /start-docs
